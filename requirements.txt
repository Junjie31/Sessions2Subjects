# GPU training pipeline (the motivating case).
# For the CPU-only analysis tier, use requirements-analysis.txt instead.
torch>=2.0
einops
timm==0.9.16
yacs
PyYAML
tqdm
opencv-python
scikit-learn
tensorboardX

# --- the selective-scan CUDA kernels (required by rppg/mamba_ssm_scan) ---
# `mamba-ssm` provides the compiled `selective_scan_cuda` extension and the
# original `mamba_ssm` modules that the fusion stem imports. The oscillatory-A
# fork lives in rppg/mamba_ssm_scan and reuses those kernels.
mamba-ssm==2.2.2
causal-conv1d==1.4.0
